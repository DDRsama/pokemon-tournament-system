# Pokemon Tournament System 3.3.1

## 中文

- 明确私有字体目录：`/data/fonts/zh`、`/data/fonts/en`、`/data/fonts/ja`。
- 切换界面语言时同步切换全局字体，日文界面优先使用日文字体。

## English

- Clarified private font folders: `/data/fonts/zh`, `/data/fonts/en`, and `/data/fonts/ja`.
- Switching the UI language now also switches the global font stack, with Japanese UI preferring Japanese fonts.

## 日本語

- 私有フォントの配置先を `/data/fonts/zh`、`/data/fonts/en`、`/data/fonts/ja` に明確化しました。
- UI 言語の切り替えに合わせて全体のフォントも切り替え、日本語 UI では日本語フォントを優先します。
